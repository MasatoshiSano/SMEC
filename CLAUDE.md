# CLAUDE.md

このファイルは、次回以降のセッションでこのリポジトリを扱う際に読むべき前提情報をまとめたもの。

## リポジトリの目的

中小企業診断士試験（日本の国家資格）の合格に必要な情報を、公式情報源を中心に収集・整理し、将来的に参考資料・問題集を作成するためのリポジトリ。まず `README.md` を読むこと。全情報の出典は `docs/05_sources.md` に一元管理している。

## 運用ルール

- 開発は `claude/shindan-shi-exam-prep-oka748` ブランチで行い、完了したら `main` にマージしてpushする（このリポジトリのオーナーの指示による運用）。
- 過去問・出題の趣旨・統計・白書などのPDFは公式サイトの無償公開資料をそのまま保存したもの。個人の学習目的の利用を前提とし、再配布・商用利用はしない。市販教材（TAC、LEC、ふぞろいな合格答案等）は著作物のため実体を含めず、書誌情報とリンクのみ記録する（`docs/04_study_materials_guide.md`）。
- PDFからテキストを抽出する際は `pdftotext -layout` を基本とする。文字化けする場合は年度によりフォント埋め込み方式が異なるため、`poppler-data` パッケージの不足や暗号化（`qpdf --decrypt`で確認）を疑う。それでも読めない場合は `pdftoppm` で画像化し `tesseract -l jpn` でOCRする（平成17年度の出題の趣旨で実績あり、認識誤りを含む参考用として保存）。

## 現在の進捗状況（2026-09-13時点）

### 完了しているもの

1. **試験制度・シラバス**：`docs/00`〜`docs/03` に試験制度全体・1次シラバス全文・2次概要・合格率統計（第1次:平成20〜令和7年度、第2次:平成19〜令和7年度）を整理済み。令和8年度からの**口述試験廃止**などの制度変更を反映済み。
2. **過去問**：第1次試験（平成19〜令和8年度、7科目）、第2次試験問題（平成19〜令和7年度、事例I〜IV）、第2次「出題の趣旨」（**平成16〜令和7年度、全年度網羅**）を収集済み（`past_exams/`）。
3. **周辺資料**：正解訂正情報（令和3〜7年度、`docs/09_answer_corrections.md`）、過去の試験案内アーカイブ、中小企業白書（2022〜2026年版、`references/whitepaper/`）、重要公式・フレームワーク集（`docs/07`）、2次試験の解答プロセス（`docs/08`）。
4. **問題集フェーズ1・2（完了）**：`problem_sets/1st_stage/` に7科目（A〜G）の論点別見出しを作成し、2016〜2025年度（直近10年）の過去問全問を分析して各論点に頻出ランク（A/B/C）と出題年度を確定済み。作業は科目ごとにサブエージェントを並列実行して行った（各科目 `problem_sets/1st_stage/<記号>_<科目名>.md` を参照）。
5. **問題集フェーズ3（一巡完了）**：`problem_sets/1st_stage/exercises/` に7科目×6問（計42問）の演習を作成済み。B・C科目でフォーマットを試作しユーザー承認を得た後、A・D・E・F・Gに同じフォーマット（設問→`<details>`で折りたたんだ解答・全選択肢の解説→関連知識）で展開した。各科目6問は代表例であり、Aランク論点はまだ多数未収録。展開時はサブエージェントに既存演習ファイルをテンプレートとして渡し「不確かな設問は避け正確性を最優先」と明示する方法が有効（計算問題は作成者が検算して確認済み）。
6. **学習スケジュール**：`docs/10_study_schedule.md` に次回試験（令和9年度想定）から逆算した5フェーズの学習計画・科目別優先順位を作成済み。
7. **初心者向け教科書（7科目完成）**：`docs/textbook/<記号>_*_textbook.md` に第1次試験7科目（A:経済学36論点、B:財務・会計34論点、C:企業経営理論42論点、D:運営管理35論点、E:経営法務27論点、F:経営情報システム24論点、G:中小企業経営・政策28論点、計226論点）の初心者向け解説を作成済み。C科目で試作したフォーマット（基本概念→具体例→ひっかけポイント→過去問で確認する、の繰り返し）を全科目に展開。計算問題は既存演習ファイルの数値と整合するよう作成・検算し、専門知識はWeb検索で正確性を確認した。ユーザーからの要求水準は「これを読んで理解すれば、問題集が100点取れる資料」。
8. **スライドテンプレートの仕様確定（デザイン検証完了）**：教科書を図解の多いスライドにする方向性を検討し、5デザイン案（Swiss Grid／Soft Pastel／Editorial Serif／Blueprint／Neubrutalist／Ink&Washi）から**Swiss Grid**（モノクロ+赤1色、罫線グリッド）を採用。汎用図解パターン12種＋科目別専用グラフ21種（経済学6・財務3・企業経営3・運営管理3・情報3）を試作し、仕様を `docs/11_slide_template_spec.md` に確定。サンプル実体は `docs/slide_template/*.html`（単体HTMLでブラウザ直接閲覧可）。
   - **重要な実装教訓**：スマホ実機での確認で「グラフがヘッダーに重なる」「文字がはみ出す」不具合が繰り返し発生し、原因を特定して修正済み。①カードの16:9固定は`aspect-ratio`プロパティ単体でなく`padding-top`%トリックで実装する、②カード内の文字サイズは固定pxでなくCSSコンテナクエリ（`cqw`単位、`container-type:inline-size`）でカード幅に追従させる、③SVGグラフの枠は高さを強制せず`object-fit:contain`と同じ考え方で「収まるだけ縮む」実装にする（`max-width:100%;max-height:100%;width:auto;height:auto`）。次回スライド関連の実装をする際は必ず `docs/11_slide_template_spec.md` の「実装上の注意点」を読んでから着手すること。
   - なお `scripts/slidegen/`（python-pptx製）でPowerPoint版も先行試作したが、生成物がPowerPointで「修復が必要」エラーになる不具合が未解決のままHTML版に方針転換した経緯がある。
   - このデザイン検討の全経緯（却下したデザイン案、モバイル実機バグの3段階にわたる原因究明の過程など）は `docs/12_slide_design_session_log.md` に詳細を記録している。次回スライド関連の作業をする前に一読すること。
9. **文章主体パターン（T1〜T3）の確定**：図解パターンの試作を終えた後、「教科書の論点の大半はグラフで描けるものではなく文章中心」という点に気づき、図解を小さく添えるだけ／図解なしの3パターン（T1:概念解説＋小アイコン、T2:よくある混同の整理＋警告マーク、T3:罫線＋番号だけの箇条書きリスト）をC科目の実内容（ドメイン、CSR/CSV、モチベーション理論）で試作し `docs/slide_template/text_primary_3.html` として確定。本番スライド作成では論点の性質に応じて図解主体（12汎用＋科目別グラフ）と文章主体（T1〜T3）を使い分ける方針を `docs/11_slide_template_spec.md` に明記した。
   - **実装上の教訓（グラフと同一クラスのバグ）**：小アイコンや箇条書き行でも「固定の高さ・幅」「`justify-content:center`」を使うと、グラフの時と全く同じ原因（縮まずあふれ、上＝ヘッダー側に重なる）でモバイル実機バグが再発した。修正も同じ原則（`height:100%`＋`max-height`でアイコンを親に追従させる、`justify-content:flex-start`＋`overflow:hidden`＋`min-height:0`で万一のあふれを下端に逃がす）で解決した。
   - **この環境でのビジュアル自己検証の発見**：Node.js版Playwright＋Chromium（`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`）がこの環境で使用可能（`npm install playwright`でセットアップ、ブラウザ再ダウンロード不要）。ユーザーにスクリーンショットを撮ってもらわなくても、自分でHTMLをレンダリング・スクリーンショットして確認できる。次回以降、スライド関連のレンダリング確認をする際はこの方法を使うこと（LibreOffice/`soffice`はこの環境で動作しないので注意、これとは別の話）。
10. **「頻出データ帯」の考案と白空間問題の解決**：一通りのパターンが揃った後、ユーザーから「厳しく評価して」と求められ、既存サンプル（4P、SWOT、需要供給曲線等）を見返した結果「カードの上半分〜6割近くが空白」という弱点を正直に指摘した。対策として`docs/textbook/C_business_administration_textbook.md`のC-7・C-8を**要約せず全文そのまま**流し込んで検証したところ、白空間の原因はモック文章が短いからではなく**16:9という箱が1論点あたりの文章量に対して構造的に大きい**ことだと判明。ユーザーの「スライドの16:9は絶対維持して、情報量を増やして」という指示を受け、`problem_sets/1st_stage/<科目>.md`に既にある実データ（頻出ランクA/B/C・出題年度）を使った「頻出データ帯」（頻出ランクチップ＋関連論点＋2016〜2025年度10年分を●/○で示す出題年表）を全パターン共通の標準要素として追加し、`docs/slide_template/freq_data_bar.html`として確定。捏造データではなく実データで埋めたことで白空間と情報価値の両方を解決した。
   - **不採用にした案**：白空間対策として、文章主体パターンを左右2列・上下2段に分割して情報量を倍にする「T4/T5」も試作したが、文字を大幅に縮小しないと収まらず可読性を犠牲にするため不採用とした（頻出データ帯だけで十分解決できたため）。
   - **教訓**：`cqw`は「どの要素のコンテナクエリを基準にしているか」で意味が変わる（列を分割したら列自体を新たなコンテナにしないと文字は縮まない、かつcqwは幅基準なので高さ方向の分割には効かない）。また、モックの見た目だけで判断せず、早い段階で実データ（要約せず全文）を流し込んで検証すること。詳細経緯は`docs/12_slide_design_session_log.md`の10章・11章参照。
11. **区切りスライド（D1・D2）の追加と日本語改行の教訓**：頻出データ帯まで確定した後もユーザーから「デザインが単調でイマイチ」という評価があり、原因は色数の少なさではなく「全スライドが同じ強さで並びリズムがないこと」と分析。色数を増やさず黒背景＋白文字の反転だけでリズムを作る区切りスライド2種（D1:科目の表紙、D2:大分類の区切り＋巨大ゴースト番号）を試作し`docs/slide_template/section_dividers.html`として確定。実データ（収録論点数・頻出ランク傾向等）も添えて装飾だけで終わらせないようにした。
    - **日本語改行の教訓**：試作時、見出し級の大きな文字で「1次試験」が「1次試」／「験で」のように単語の途中で改行される問題が発生。ユーザーの「スマホだから？」という質問に対し、デスクトップでも同じ崩れが起きることを確認し、**スマホ特有の不具合ではなく、日本語には単語間スペースがなくブラウザがどの文字間でも改行してよいと扱うため、レスポンシブな幅では折り返し位置がたまたま単語の途中に来ることがある**という一般的な現象だと判明。該当単語を`white-space:nowrap`のspanで囲んで解決した。今後、見出し・サブタイトルなど大きな文字を書く際は、割れると読みにくい熟語・複合語をチェックすること。詳細は`docs/12_slide_design_session_log.md`の11章参照。
12. **本番第1弾（企業経営理論 表紙〜C-3）作成と、用語ラベル`.term-row .k`の幅問題（2段階で修正）**：実データのみで表紙・区切り・C-1〜C-3の5枚を作成したが、自分のPlaywright環境では問題なくてもユーザーの実際のブラウザで「VRIOのラベルと説明文が被っている」という指摘を受けた。1回目の対処（幅を1.5倍に広げ、nowrapを外して折り返し可能にする）は逆効果で、ユーザー環境ではラベルが実際に折り返して縦スペースを消費し、後続の内容（I・O、警告文、具体例、ひっかけ）が丸ごと非表示になる**より深刻な不具合**を生んだ。自分の環境（複数幅・フォント読み込み失敗条件）では再現できなかったため、余白を推測で調整するのをやめ、**ラベル列の幅を一切固定せず（`flex:0 0 auto`のみ）、`white-space:nowrap`で折り返しも禁止する**（内容の文字数にぴったり合わせる）方式に根本修正した。列としての整列は多少崩れるが、はみ出し・消失は原理的に発生しなくなる。**教訓**：①横方向のはみ出しを防ぐための「折り返し許可」が、縦方向のスペースを圧迫して`overflow:hidden`と組み合わさり内容消失を招くことがある、②再現できない不具合は余白の推測で対処せず、固定幅への依存自体をなくせないか検討する、③1つの検証環境・1解像度での確認は不十分、デスクトップ・中間幅・モバイルの3幅で確認する。詳細は`docs/12_slide_design_session_log.md`の13〜16章参照。

13. **「過去問チェック」スライドの導入とコンテンツ抜け漏れの発見**：ユーザーから「内容スライドの直後に過去問の出題例を置き、それに答えられなければ内容スライドが不足している証拠にする」という提案を受け、`problem_sets/1st_stage/exercises/`の実在の過去問（令和7年度）とC-1・C-2の内容スライドを照合した。結果、C-1では過去問の選択肢に対応する説明（模倣困難性の3源泉のうち「社会的複雑性」、Vとの区別）が欠けていること、C-2では過去問が実際には教科書に独立した解説がない「アンゾフの成長マトリクス」を要求していることが判明し、それぞれ修正・補足スライド追加で対応した。過去問チェックスライドは`slides/1st_stage/C_business_administration.html`に標準コンポーネントとして実装し、`problem_sets/1st_stage/exercises/`に演習がまだない論点では**過去問を捏造せずスキップする**運用にした。
    - **はみ出しバグとQ/A分割による解決**：設問＋選択肢＋解説を1枚に詰め込んだところ、自分の環境では問題なくてもユーザーの実際のブラウザで選択肢後半・解説が見切れる不具合が発生（用語ラベル幅の問題と同じく、自分の環境では再現できなかった）。原因追求よりも構造的解決を優先し、ユーザーの「回答は隠して、回答＆解説ページを分けて」という提案も踏まえて**「設問のみ」と「解答＆解説」の2枚に分割**。情報量を半分にすることではみ出しリスクを下げつつ、「答えを見る前に考える」というUXも同時に実現した。`<details>`のクリック開閉は固定高さ(16:9)のスライドでは`overflow:hidden`により機能しないため採用していない。
14. **`cqw`（コンテナクエリ）を全廃し`calc()`＋CSS変数に置き換えた（根本原因への対応）**：Q/A分割後もユーザーから再度「下にはみ出す・文字が欠ける」報告があり（アンゾフの成長マトリクス、C-2過去問設問の両方）、これまで3回起きた不具合（用語ラベルの重なり／その修正が招いた内容消失／過去問チェックのはみ出し）に共通するのは**すべて`cqw`を使っている箇所**だと気づいた。ユーザーの閲覧環境で`container-type`/`cqw`（コンテナクエリ）自体が効いておらず、指定が丸ごと無効になってブラウザの既定フォントサイズ（意図の2倍近く）にフォールバックしていた可能性が高いと判断。原因を完全には確証できなかったが、**`cqw`をやめて`calc(var(--cw)*N/100)`（`--cw:calc(min(100vw,1020px) - 48px)`という2016年前後から広く対応するCSS変数+calc()+min()+vwの組み合わせ）に全面置換**した（ダウンサイドのない安全な代替のため）。`slides/1st_stage/C_business_administration.html`と`docs/slide_template/`全9ファイルをスクリプトで一括変換し、`container-type:inline-size`も削除。自分の環境でのレンダリング結果は変換前と完全一致し視覚的回帰なしを確認。**今後、新しいスライドコンポーネントを作る際は`cqw`を使わず、最初から`calc(var(--cw)*N/100)`を使うこと。**
15. **`calc()`化しても再発、真犯人は`text-size-adjust`（モバイル自動文字拡大）だった可能性**：`calc()`化後もユーザーから同じ症状（過去問チェックの選択肢が下で欠ける）の再発報告があり、`calc()`・CSS変数・`vw`は極めて広く対応しているためコンテナクエリ非対応という前回の仮説では説明がつかないと判断。行き着いたのは、モバイルブラウザの「自動文字拡大（`text-size-adjust`、フォントブースティング）」──レイアウトの箱は指定通りのフォントサイズで計算されるが、描画される文字だけが読みやすさのため自動的に拡大される機能で、指定単位（px/cqw/calc()のいずれ）を問わず作用し、Playwright/Chromiumのデスクトップ・ヘッドレス環境では再現しない。`html{-webkit-text-size-adjust:100%;-moz-text-size-adjust:100%;text-size-adjust:100%;}`で無効化し、デスクトップの見た目に影響しないことを確認して全ファイルに追加した。**新しいHTMLファイルを作る際は必ずこの1行を`<style>`の先頭付近に入れること。** 教訓：1つの仮説で直らなかったら、その仮説自体を疑い一から容疑者を洗い直す。「自分の環境では絶対に再現しないのに実機では毎回崩れる」不具合は、CSSの書き方よりも先にブラウザが黙って行う自動調整（`text-size-adjust`等）を疑う。詳細は`docs/12_slide_design_session_log.md`の17〜24章参照。
16. **「リンクが開けない」の真因はURL直後の日本語だった**：チャット返信でURLの直後に空白・改行を挟まず「（更新版）」等の日本語を続けて書くと、URL自動リンク処理にその文言が巻き込まれてリンクが壊れることが判明。これまでの「開けない」報告の一部はスライドの不具合ではなくこれが原因だった可能性がある。**URLの直後には必ず改行か空白を入れ、文字を続けて書かないこと。** この気づきでリンクが正常に開けるようになった結果、`.matrix-rows`（アンゾフの成長マトリクス）で`.term-row .k`と同じ「固定幅＋日本語ラベル」バグ（`.row .name`・`.row .combo`）が別コンポーネントとして見落とされていたことも発覚し、同じ修正（`flex:0 0 auto`＋`white-space:nowrap`）を適用した。教訓：1つの不具合パターンを直したら、同じCSS設計（`flex:0 0 calc(var(--cw)*N/100)`のような固定幅パターン）を使う他の箇所がないか横断的に`grep`で確認すること。詳細は`docs/12_slide_design_session_log.md`の25〜26章参照。
17. **繰り返した実機不具合の真因は`<meta name="viewport">`タグの完全な欠落だった**：`.matrix-rows`修正後もexamqスライド（C-2過去問チェック「設問」、選択肢エが下で欠ける）の症状が再発し、`cqw`→`calc()`化・`text-size-adjust:100%`という2段階の対策では解決しなかった。`AskUserQuestion`で確認したところユーザーはAndroid／Chromeでの閲覧。原因調査の結果、本番ファイルと`docs/slide_template/`配下の全9ファイル、計10本に`<meta name="viewport" content="width=device-width, initial-scale=1">`タグが一つもないことが判明。このタグがないと実機のモバイルブラウザは仮想デスクトップ幅（約980px）でレイアウトしてからズームアウトして表示するため、`calc(var(--cw)*N/100)`という相対計算式自体は正しくても`--cw`が実際の画面幅ではなく仮想980px基準で計算され、ユーザーの画面は実質的に自分がPlaywrightで検証していた1400px幅のデスクトップ表示と同じ崩れ方をしていた。Playwrightの`viewport`オプションは常にCDP経由で指定幅を強制するため、このタグ欠落によるフォールバック動作を原理的に再現できず、どれだけ狭い幅で検証しても再現しなかった。全10ファイルの`<title>`直後にこのタグを追加し、Playwrightで393px幅シミュレーション時に過去問チェックの全選択肢がカード内に収まることを確認した。**新しいHTMLファイルを作る際は`<title>`の直後に必ずこのタグを入れること。** 教訓：同じ症状が対策を重ねても再発する場合、単位やプロパティの精緻化を繰り返す前に`<head>`の基本タグ（viewport等）の欠落を最初に疑うこと。また、Playwrightの明示的viewport指定は「タグなし時の実機フォールバック」を再現できない検証手法上の盲点であることを認識しておく。詳細は`docs/12_slide_design_session_log.md`の27〜28章参照。
18. **PowerPoint版スライドのテンプレート化（`scripts/pptx_slidegen/`）**：ユーザーから「スライドをPowerPointでダウンロードできるか」と聞かれ、`pptxgenjs`で企業経営理論10枚（表紙〜C-3）を新規作成した（`scripts/slidegen/`＝python-pptx製の旧試作は「修復が必要」エラーで放棄済みのため、別の新規実装）。この環境では`soffice`によるpptx→PDF変換が機能しない（`.txt`ですら失敗）ため目視レンダリング確認ができず、代わりに①`scripts/office/validate.py`によるスキーマ検証、②python-pptxでの全シェイプ境界チェック、③全角/半角文字幅からの折り返し行数見積もりチェック、の3点セットで検証する方式を確立した。この見積もりチェックで実際に複数箇所の高さ不足（「具体例」ボックス等）を検出・修正できた。また、Google Slidesで確認したユーザーから「HTML版と全然違う」と指摘を受け調査したところ、区切りスライドの半透明の白文字（`transparency`オプション）がGoogle Slidesでは完全不透明にしか表示されないことが判明（OOXML自体は正しく`<a:alpha>`を書き込んでいたため、Google Slides側のレンダリング制約）。`transparency`に頼らず、背景色`#171717`との事前混色で実色（`W_10`〜`W_94`という定数）を作る方式に切り替え、閲覧環境を問わず同じ見た目になるようにした。さらにユーザーの「左に図解、右にテキスト」という要望に応じて2カラムレイアウトを実装した後、「内容に合わせて図解の要否を判断するように」との指示を受け、C-1（VRIOは用語リストで足りるため図解なし）・C-2（視野の広さの比較は図解が有効）・C-3（円の重なり＝シナジーはまさに図解向き）という判断基準を確立した。最後にユーザーから「テンプレートとしてできているか」と問われ、ベタ書きだった1本のスクリプトを`scripts/pptx_slidegen/lib.js`（再利用可能な部品：`addCoverSlide`・`addDividerSlide`・`addHeader`・`addFreqBar`・`addTermRows`・`addRowList`・`addExamQuestion`・`drawScopeBars`・`drawVennOverlap`）と`build_C_business_administration.js`（内容定義のみ）に分離し、C-4以降や他科目のデッキも同じ部品を呼び出すだけで作れる構成にした。詳細な使い方・座標基準・図解要否の判断基準は`docs/13_pptx_slide_template_spec.md`参照。
19. **企業経営理論（C科目）42論点、HTML・PowerPoint両方が完成**：C-4〜C-14（経営戦略論）、C-15〜C-29（組織論）、C-30〜C-42（マーケティング論）を順次追加し、`slides/1st_stage/C_business_administration.html`（57枚）と`slides/1st_stage/C_business_administration.pptx`（57枚、`scripts/pptx_slidegen/build_C_business_administration.js`で生成）が両方とも表紙〜C-42まで完成。過去問の実データが存在する論点（C-1・C-2・C-5・C-9・C-10）にのみ過去問チェック（examq）スライドを追加し、存在しない論点は捏造せずスキップする運用を最後まで一貫させた。C-24（組織変革・レヴィンの3段階モデル）・C-36（PLC）では新規に`process-steps`（HTML）／`drawProcessSteps`（`lib.js`、PPTX）という横並び手順図コンポーネントを追加し、両実装間で見た目を一致させた。図解の要否判断（構造的に空間・比較・連続性がある論点にのみ図解を使う）は最後まで一貫して適用し、大半の論点は文章主体（`icon-wrap`/`prose`または`matrix-rows`一覧、PPTX側は`addTermRows`/`addRowList`）で構成した。HTML版はPlaywrightで1400px・393px幅の両方ではみ出しゼロを確認、PPTX版は`validate.py`（スキーマ検証）＋自作の`check_deck.py`（境界チェック・テキストボックス同士のオーバーラップ検出・全角/半角文字幅からの折り返し行数見積もりの3点）で検証する運用を毎バッチ徹底した（区切りスライドのゴースト番号と副題テキストが意図的に重なる仕様上の1パターンのみ、既知の無害な誤検知として除外）。**次に他科目（A・B・D・E・F・G）でスライドを作る際は、この一連のコミット（`git log`でC-4〜C-42追加分を参照）とdocs/11・docs/13を先に読み、同じワークフロー（HTML先行→Playwright検証→コミット/プッシュ/マージ→PPTX移植→3点チェック→コミット/プッシュ/マージ→ユーザーへ都度ファイル送付）をそのまま踏襲すること。**

20. **「左2/3テキスト＋右1/3図解」の設計ドリフトを発見・是正**：ユーザーから「内容次第で右1/3に図解を入れる話だったが、結果的に全く入ってない？」と指摘され調査したところ、この方針が実際に機能していたのはPowerPoint版のC-2・C-3の2枚だけで、HTML版には一度も実装されていなかったことが判明（HTML版はC-2・C-3も含め全論点が「小さい装飾アイコン＋文章」か「全幅図解（説明文なし）」のどちらかだった）。ユーザーに`AskUserQuestion`で確認の上、7論点（C-2ドメイン、C-3多角化、C-4 PPM、C-6 5フォース、C-7競争優位の3戦略、C-24組織変革、C-36 PLC）をHTML・PPTX両方で「左2/3テキスト＋右1/3図解」に統一する対応で合意し、実施した。
    - **右カラムの図解実装方式**：`calc(var(--cw)*N/100)`はカード全体幅基準の値で、狭い右カラム（全体の1/3程度）にそのまま使うと文字が大きすぎて溢れる（cqw問題の変種）。これを避けるため、右カラムの図解は`.icon-mark svg`と全く同じ「viewBox付きSVG＋`width:auto;height:auto;max-width:100%;max-height:100%`」方式（新設した`.split-wrap`/`.split-diagram`）で実装した。SVG内のテキストはviewBoxに対する相対座標なので、コンテナがどれだけ狭くなっても自動的に一様スケールし、この種の「狭いカラムでの文字溢れ」問題が原理的に起きない。**右カラムに新しい図解を追加する際は、今後もこの「SVG viewBox方式」を最優先で使うこと（`calc(var(--cw)*N/100)`ベースの新規コンポーネントを狭いカラムに直接流用しない）。**
    - **横並びの手順図は狭い列に収まらない**：C-24・C-36は元々`process-steps`（横並び3〜4ステップ＋矢印）だったが、右1/3幅（全体の約33%）に横並びで詰め込むと1ステップあたりの幅が狭すぎる。縦積み＋下矢印の新パターンに設計し直し、HTML側は同じviewBox SVG方式で、PPTX側は`lib.js`に`drawProcessStepsVertical`を新規追加して対応した。
    - **PPTXは元からC-2・C-3のみ2カラムだった**：`drawScopeBars`（C-2用）・`drawVennOverlap`（C-3用）はこの2枚のためにすでに存在しており、これ自体は正しく機能していた。今回はこの2枚の文言をHTML版の簡潔な文章に合わせて整理し、C-4・C-7には既存の`drawQuadrant`、C-6には既存の`draw5Forces`を右カラム幅（4.05in）向けの短いラベルで再利用した（関数自体の変更は不要だった）。
    - **教訓**：「内容に応じて図解を入れる／入れない」という判断基準を確立しても、実装が進むにつれて別の理由（狭い列に収まらない等）でこっそり別の形（全幅図解や図解なし）に倒れてしまうことがある。数十枚規模の作業をバッチで進めた後は、当初の設計方針が実際にどれだけ体現されているかをたまに棚卸しし、HTML版とPPTX版の実装が食い違っていないかも確認するとよい。

21. **経済学・経済政策（A科目）36論点、HTML・PowerPoint両方が完成**：C科目（企業経営理論）に続く2科目目として、A-1〜A-6（経済指標）・A-7〜A-17（マクロ経済理論・国際経済）・A-18〜A-24（主要経済理論・市場メカニズム）・A-25〜A-36（消費と生産の理論・組織と戦略の経済学・所得分配）の4パートを順次追加し、`slides/1st_stage/A_economics.html`（53枚）と`slides/1st_stage/A_economics.pptx`（53枚、`scripts/pptx_slidegen/build_A_economics.js`で生成）が両方とも完成。過去問の実データが存在する論点（A-9・A-13・A-14・A-16・A-22・A-30）にのみ過去問チェックを追加し、頻出ランク・出題年度が未集計の論点（A-18・A-20）は捏造せず「－／集計データなし」と誠実に表示する運用を新たに確立した（`problem_sets/1st_stage/A_economics.md`の該当行が空欄だったため）。
    - **経済学専用グラフをHTML→PPTXへ本番投入**：`docs/slide_template/economics_6.html`で試作済みだった6パターン（需要供給曲線・曲線シフト・45度線分析・IS-LM分析・余剰と死荷重・景気循環）のうち、HTML側は座標軸＋直線・曲線のSVGをそのまま実データで流し込んで使用（`.gside`/`.tside`＝図メイン＋右に凡例、`.gcap-wrap`＝図中央＋下にキャプション、という2レイアウトをCSSに追加）。PPTX側は**pptxgenjsに滑らかな曲線を描く手段がない**ため、`lib.js`に`drawLineChart`（正規化座標0〜1で指定した直線群＋軸＋均衡点を描く汎用関数。`drawLineSeg`ヘルパーが対角線をpptxgenjsの`line`シェイプ＋`flipV`で表現する）を新規追加し、教科書の慣例どおり直線で近似する方針にした。景気循環（A-13）だけは滑らかな波形が本質でなく「周期の長さ×原因」という一覧情報の方が試験で問われる内容そのものだったため、PPTX版は曲線を諦め`addRowList`の一覧表に置き換えた（HTML版は曲線のまま）。経済余剰・死荷重（A-23）の死荷重三角形も、正確な図形化はpptxgenjsの基本図形では再現コストが高いため、PPTX版はD/S直線＋均衡点の図解に凡例テキスト（■消費者余剰／■生産者余剰／■死荷重）を添える簡略構成にとどめた（内容の正確性は保持、装飾的な図形の精密さのみ妥協）。**今後、他科目で座標軸グラフが必要な論点が出てきたら、まず`drawLineChart`の使い回しで足りないか検討すること。**
    - **PPTXチェックで新たに検出したパターン2件を是正**：①`addExamQuestion`（共通コンポーネント）の設問文（stem）ボックスの高さが1.0in固定で、a・b・c・dの4選択肢を含む長い設問文（改行込み）だと収まらないケースがあったため、`stemH`パラメータを追加して呼び出し側で調整できるようにした。②`addDividerSlide`・`addHeader`のタイトルボックスも固定高さのため、26〜27文字級の長い日本語タイトルが2行に折り返され収まらないケースがあり、該当タイトルを意味を保ったまま短縮して対処した（ボックス自体は変更せず、C科目を含む他デッキへの影響を避けた）。**長めの見出し文を書くときは、既存の同フォントサイズの見出しが何文字まで収まっているかを目安に、都度確認すること。**

22. **組織論・マーケティング論の過去問チェックスライドが欠落していた問題を解消（C科目 57枚→81枚）**：ユーザーから「マーケティングなどで『こう聞かれる』の過去問が全くでてこなくなった」「組織論も」と指摘を受け調査した結果、`problem_sets/1st_stage/exercises/C_business_administration_exercises.md`に組織論（C-15〜C-29）・マーケティング論（C-30〜C-42）向けの実在過去問が1問も存在しないことが根本原因と判明（捏造しない運用を徹底した結果として演習が存在せず、過去問チェックスライド自体を作れていなかった）。令和5〜7年度のC科目過去問（`past_exams/1st_stage/1ji{2023,2024,2025}/C1JI{year}.pdf`）から、正解を`past_exams/1st_stage_answers/`の該当年度PDFで検証し、全選択肢を教科書記載事項または一般に確立された経営学・マーケティング論の知識で説明できることを確認した上で、組織論6問（C-15分業と調整、C-16組織構造、C-19目標設定理論、C-21コンフリクト、C-23組織間関係、C-24組織変革への抵抗〔不適切問題〕）・マーケティング論6問（C-30顧客価値、C-32マーケティング・リサーチ、C-37ブランディング、C-38消費者行動、C-39プライシング、C-40コミュニケーション）を選定・追加した。
    - **「教科書に書いていない知識は使わない」ではなく「教科書に書いていない知識は解説の中で正確に補う」方針へ**：既存のC-1（VRIO）演習がすでに教科書本文を超える知識（模倣困難性の3源泉等）を解説内で使っていた前例を確認し、それに倣った。ただし今回、C-20リーダーシップ理論（令和7年度第20問、パス・ゴール理論等）は教科書が全くカバーしていない複数の未知理論を選択肢が要求しており、正確な解説を書く自信が持てなかったため不採用として別の候補を探した。**「教科書ページが薄い＝使えない」ではなく「正確に説明できる自信があるか」で採否を判断する**、という基準を明確化した。
    - **スライド挿入・全面リナンバーをPythonスクリプトで自動化**：81枚（HTML）・81枚（PPTX）という規模で、12論点×2枚（設問／解答＆解説）を任意の位置に挿入しつつ全スライド番号を振り直す作業は手作業では誤りやすいため、HTML・PPTX（JS）それぞれについて「一意なアンカー文字列（スライド見出しテキスト／`// ---------- Slide N: ...`コメント）を検索→次のスライド境界の直前に新規ブロックを挿入→全`slide-no`または`// ---------- Slide`コメントを1から振り直す」という汎用Pythonスクリプトを書いて実行する方式を確立した。**今後、既存デッキの中間に新しいスライドをまとめて挿入する際は、手作業でのリナンバーではなくこの自動化パターン（アンカー検索＋一括リナンバー）を再利用すること。**
    - **PPTX版`addExamQuestion`の選択肢ボックスに可変高さオプションを追加**：選択肢ボックスの高さは1行あたり固定0.52inだったため、2行を超える長文選択肢（プライシングの学割・ダイナミックプライシング比較、顧客価値の本質機能・付随機能の説明等）で`check_deck.py`のTEXT OVERFLOW ESTIMATEに引っかかった。`stemH`パラメータ（設問文ボックス用、既存）と同じ考え方で、選択肢オブジェクトに`h`フィールド（例：`{ badge: "ウ", text: "...", h: 0.72 }`）を追加できるよう`lib.js`の`addExamQuestion`を拡張し、該当する選択肢だけ高さを上書きした。高さを増やした分は後続の解答・解説ボックスの開始位置がその分下がるため、全体が7.5in（16:9スライド高さ）に収まるよう解答スライド側の見出し間の余白（`cy +=`の値）も併せて詰めて調整した。**長文選択肢で折り返し行数が超過した場合は、選択肢全体のフォントサイズを下げるのではなく、その選択肢だけ`h`で個別に高さを増やし、必要なら周辺の余白を詰めて帳尻を合わせるのがこの科目の標準対応。**

23. **財務・会計（B科目）34論点、HTML・PowerPoint両方が完成**：A科目（経済学）・C科目（企業経営理論）に続く3科目目として、B-1〜B-11（簿記・会計の基礎、原価計算）・B-12〜B-19（経営分析、利益と資金の管理）・B-20〜B-27（資金調達と配当政策、実物投資）・B-28〜B-34（証券投資、企業価値、デリバティブ）の4パートを順次追加し、`slides/1st_stage/B_finance_accounting.html`（51枚）と`slides/1st_stage/B_finance_accounting.pptx`（51枚、`scripts/pptx_slidegen/build_B_finance_accounting.js`）が両方とも完成。既存の`problem_sets/1st_stage/exercises/B_finance_accounting_exercises.md`（フェーズ3試作時点で6問収録済み）の実データのみを使って過去問チェックを4論点（B-9税効果会計、B-15営業レバレッジ、B-19フリーキャッシュフロー、B-21資本コスト×2問、B-26NPV法）に追加し、対応する演習がない論点は捏造せずスキップする運用を継続した。
    - **財務・会計特有のグラフをHTML・PPTX両方で実装**：CVP分析（B-15、損益分岐点）は売上高線・総原価線・BEPを交点で示すグラフをHTML（SVG）はゼロから、PPTXはA科目で新設した`drawLineChart`をそのまま再利用して実装（設計時点の想定どおり流用できた）。貸借対照表の均衡（B-5、資産＝負債＋純資産）、DCFのタイムライン（B-24、将来CFを現在価値に割り引く矢印図）、オプションの損益図（B-33、コール買いのホッケースティック型グラフ）は、この科目で新たに必要になった図解パターンとして追加した。オプション損益図のような「途中で傾きが変わる折れ線」はPPTX側で`drawLineSeg`（内部ヘルパーだったが今回`module.exports`に追加して外部公開）を2回呼んで実装するのが最も簡単で、無理に新しい専用関数を作らなかった。
    - **選択肢ボックスの高さは行ごとに個別上書きできる**：examqスライドで2行に収まらない長文選択肢（学割・ダイナミックプライシングの比較など）が複数の論点で発生したため、C科目での対応時に`addExamQuestion`の選択肢オブジェクトへ追加した`h`フィールド（`{ badge, text, h: 0.72 }`のように個別指定）をそのまま活用した。高さを増やした分だけ後続の解答・解説ボックスの余白（`cy +=`の値）も詰めて7.5in（16:9スライド高さ）に収める、という調整が今回も複数箇所で必要になった。
    - **区切りスライドのタイトルは13〜14文字が2行折り返しの境界**：「資金調達と配当政策／実物投資」（14文字）でPPTX側の`addDividerSlide`タイトルボックス（固定高さ）が2行に折り返し収まらない実例が出た。C科目のときと同じ対応（ボックス自体は変えずタイトル文言だけ意味を保って短縮、HTML側はレスポンシブなので元の文言のまま）を踏襲した。**次の科目でも区切りタイトルは13文字程度を目安に収まるか確認すること。**
    - **B-3・B-25のように過去問集計データが存在しない論点への対応を確認**：`problem_sets/1st_stage/B_finance_accounting.md`でランク・出題年度が空欄の論点（B-3、B-25）は、A科目のA-18・A-20で確立した「捏造せず『－／集計データなし』と誠実に表示する」運用をそのまま適用した。

24. **運営管理（D科目）35論点、HTML・PowerPoint両方が完成**：A（経済学）・B（財務会計）・C（企業経営理論）に続く4科目目として、D-1〜D-9（生産管理概論／生産計画）・D-10〜D-19（生産統制／作業・設備・物の管理）・D-20〜D-28（在庫・品質管理／店舗・商業集積）・D-29〜D-35（価格・販促／物流・流通情報）の4パートを順次追加し、`slides/1st_stage/D_operations_management.html`（52枚）と`slides/1st_stage/D_operations_management.pptx`（52枚、`scripts/pptx_slidegen/build_D_operations_management.js`）が両方とも完成。既存の`problem_sets/1st_stage/exercises/D_operations_management_exercises.md`（フェーズ3試作時点で生産管理・店舗販売管理から3問ずつ計6問収録済み）の実データのみを使って過去問チェックを5論点（D-6ラインバランシング、D-9ジョンソンの規則、D-20在庫管理、D-21品質管理、D-24ライリーの法則、D-34アソシエーション分析）に追加した。
    - **棒グラフ用の新規コンポーネント`drawBarChart`を追加**：D-6（ラインバランシング）のネック工程を可視化するため、lib.jsに`drawBarChart`（N本の縦棒＋ラベル、特定の棒を赤でハイライト）を新規追加した。実装時、値ラベル・名前ラベルの左右への張り出し幅がバーの間隔より広く、隣接ラベル同士が重なるバグを`check_deck.py`で検出（`ox=0.06`程度のわずかな重なり）。ラベル幅の左右パディングを縮小して解消した。**今後、棒グラフが必要な論点（他科目でも）はまずこの`drawBarChart`の流用を検討すること。**
    - **「組み合わせ」形式と「1設問に複数の小問」形式、2つの新しい過去問チェックパターン**：D-21（QC7つ道具）はC科目のC-21と同様の「a〜d×①〜④の組み合わせ」形式で問題なく対応できたが、D-34（アソシエーション分析）は「1つの設問文（表形式のデータ）に対し設問1・設問2という2つの小問が続く」形式で、既存の`addExamQuestion`（1つのstemと1組の選択肢を前提とする設計）では表現できなかった。この場合は共有ヘルパーを使わず、スライドごとにカスタムレイアウト（設問ごとにラベル＋文章を積み重ねる）で個別対応するのが最も簡単だった。**「1設問1小問」を前提とする既存コンポーネントに無理に当てはめず、共有関数の対象外になる出題形式が出てきたら都度カスタムコードで対応してよい**、という判断基準が明確になった。
    - **D-15（設備の評価と更新）のように過去問集計データが存在しない論点への対応を継続**：A科目・B科目で確立した「捏造せず『－／集計データなし』と誠実に表示する」運用をそのまま適用。
    - **区切りタイトルの2行折り返し対策を継続**：「生産統制／作業・設備・物の管理」（15文字）「資金調達と配当政策／実物投資」相当の長いタイトルでPPTX側`addDividerSlide`が2行に折り返し収まらない事象が今回も複数回発生し、都度タイトルをPPTX側のみ意味を保ったまま短縮（13文字目安）して対処した。HTML側はレスポンシブなため元の文言のまま。

25. **経営法務（E科目）27論点、HTML・PowerPoint両方が完成**：A（経済学）・B（財務会計）・C（企業経営理論）・D（運営管理）に続く5科目目として、E-1〜E-9（事業開始・倒産／知的財産権）・E-10〜E-19（知財契約／取引関係法務／民法・会社法）・E-20〜E-27（独禁法・消費者保護／事業承継／資本市場）の3パートを順次追加し、`slides/1st_stage/E_business_law.html`（43枚）と`slides/1st_stage/E_business_law.pptx`（43枚、`scripts/pptx_slidegen/build_E_business_law.js`）が両方とも完成。`problem_sets/1st_stage/exercises/E_business_law_exercises.md`の実データを使って過去問チェックを6論点（E-4事業譲渡と会社分割、E-6職務発明、E-7商標の不使用取消審判、E-17遺留分、E-18取締役会の運営、E-21不正競争防止法）に追加した。
    - **「－／直近10年単独出題なし」運用の継続と対象論点の多さ**：`problem_sets/1st_stage/E_business_law.md`で頻出ランク・出題年度が空欄の論点（E-1, E-3, E-20, E-24, E-25の5論点）は、他科目で確立した運用（捏造せず「－」と誠実に表示）をそのまま適用。法律科目は「制度はあるが単独では出題されない」論点（E-20金商法・E-24国際条約・E-25資本市場基礎）が他科目より多く、本文中でも「E-26と合わせて学習」のように関連論点への参照を明記する運用にした。
    - **E-8（意匠権・実用新案権の存続期間比較）で初めて棒グラフをHTML側でも直接SVG実装**：これまで`drawBarChart`（PPTX側、D科目で新設）に対応するHTML側の棒グラフは`.bar-chart`というCSS実装で用意されていたが未使用だったため、E-8では代わりに`.split-diagram`内に手書きSVG（`<rect>`＋`<text>`）で特許20年／実用新案10年／意匠25年（赤ハイライト）／商標10年+更新の4本棒グラフを実装した。初回、商標の「10年+更新」ラベルがviewBox右端をはみ出す不具合をPlaywrightで検出し、2行（"10年"＋"+更新"）に分割して解消。PPTX側は既存の`drawBarChart`をそのまま流用でき、右カラム図解の座標（`diagX=8.25, diagW=4.05`）に配置した際、図解キャプションの位置がバーの下ラベルと重なるD-6と同型のバグが再発し、同じ修正（キャプションのy座標を下げる）で対応した。**教訓：新しい棒グラフを手書きSVGで作る際は、ラベルの左右幅がviewBoxやカラム幅を超えないか、特に長い文字列（単位付き数値等）は要確認。**
    - **区切りタイトルの2行折り返し対策を継続**：PART2「知的財産権（契約）／取引関係法務／民法・会社法」（23文字）→「取引法務・民法・会社法」、PART3「独禁法・消費者保護・資本市場」（14文字）→「独禁法と消費者保護」と、他科目より長くなりがちな法律科目の区切りタイトルもPPTX側のみ意味を保ったまま短縮（10〜13文字目安）して対処した。

### 未着手・今後の拡充候補

- **演習の拡充**：`problem_sets/1st_stage/exercises/` の各科目6問を、`problem_sets/1st_stage/<科目>.md` の未収録Aランク論点に向けて追加していく。教科書側は全論点をカバー済みだが、対応する演習が手薄な論点が多いため優先順位が高い。
- **第2次試験の問題集化・教科書化**：`problem_sets/` および `docs/textbook/` は現状1次試験のみ。事例I〜IVは論述式のため論点別ランク付けになじまず、別の切り口（頻出テーマ・フレームワーク別の演習集など）を検討する必要がある。
- **令和8年度第2次試験**（2026年10月25日実施予定）の過去問・出題の趣旨・統計は実施後に追加すること。
- **スライドの他科目への展開（次のステップ）**：経済学経済政策（A科目、36論点）・財務会計（B科目、34論点）・企業経営理論（C科目、42論点）・運営管理（D科目、35論点）・経営法務（E科目、27論点）はHTML・PowerPointとも完成済み。`docs/11_slide_template_spec.md`・`docs/13_pptx_slide_template_spec.md`の仕様と、5科目で確立したワークフロー（`slides/1st_stage/{A_economics,B_finance_accounting,C_business_administration,D_operations_management,E_business_law}.html`と対応する`build_*.js`が実例）に沿って、残り2科目（F経営情報システム・G中小企業経営政策）へ展開する。各科目ごとに`slides/1st_stage/<科目名>.html`＋`scripts/pptx_slidegen/build_<科目名>.js`を新規作成する方針（`lib.js`のコンポーネントはそのまま流用可能。`drawBarChart`はD科目で新設、E-8で右カラム図解への流用実績あり）。
- **中小企業経営政策(G)の専用グラフ**：他6科目分は作成済みだが、G科目分の専用パターンは未着手（中小企業基本法の分類基準表など、政策系ならではの見せ方の検討が必要）。経営法務(E)はE-8の存続期間比較棒グラフで対応済み。

## よくある作業パターン

- 新しい年度のPDFを追加する場合：`past_exams/README.md` のファイル名対応表（A〜G＝科目、A〜D＝事例）を参照し、`docs/05_sources.md` に出典を追記すること。
- 論点別頻出ランクを再集計する場合：`problem_sets/1st_stage/<科目>.md` の「集計時の参照先」欄にPDFのパスパターンが書いてあるので、そこから辿る。
