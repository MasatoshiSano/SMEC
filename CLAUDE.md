# CLAUDE.md

このファイルは、次回以降のセッションでこのリポジトリを扱う際に読むべき前提情報をまとめたもの。

## リポジトリの目的

中小企業診断士試験（日本の国家資格）の合格に必要な情報を、公式情報源を中心に収集・整理し、将来的に参考資料・問題集を作成するためのリポジトリ。まず `README.md` を読むこと。全情報の出典は `docs/05_sources.md` に一元管理している。

## 運用ルール

- 開発は `claude/shindan-shi-exam-prep-oka748` ブランチで行い、完了したら `main` にマージしてpushする（このリポジトリのオーナーの指示による運用）。
- 過去問・出題の趣旨・統計・白書などのPDFは公式サイトの無償公開資料をそのまま保存したもの。個人の学習目的の利用を前提とし、再配布・商用利用はしない。市販教材（TAC、LEC、ふぞろいな合格答案等）は著作物のため実体を含めず、書誌情報とリンクのみ記録する（`docs/04_study_materials_guide.md`）。
- PDFからテキストを抽出する際は `pdftotext -layout` を基本とする。文字化けする場合は年度によりフォント埋め込み方式が異なるため、`poppler-data` パッケージの不足や暗号化（`qpdf --decrypt`で確認）を疑う。それでも読めない場合は `pdftoppm` で画像化し `tesseract -l jpn` でOCRする（平成17年度の出題の趣旨で実績あり、認識誤りを含む参考用として保存）。

## 現在の進捗状況（2026-09-12時点）

### 完了しているもの

1. **試験制度・シラバス**：`docs/00`〜`docs/03` に試験制度全体・1次シラバス全文・2次概要・合格率統計（第1次:平成20〜令和7年度、第2次:平成19〜令和7年度）を整理済み。令和8年度からの**口述試験廃止**などの制度変更を反映済み。
2. **過去問**：第1次試験（平成19〜令和8年度、7科目）、第2次試験問題（平成19〜令和7年度、事例I〜IV）、第2次「出題の趣旨」（**平成16〜令和7年度、全年度網羅**）を収集済み（`past_exams/`）。
3. **周辺資料**：正解訂正情報（令和3〜7年度、`docs/09_answer_corrections.md`）、過去の試験案内アーカイブ、中小企業白書（2022〜2026年版、`references/whitepaper/`）、重要公式・フレームワーク集（`docs/07`）、2次試験の解答プロセス（`docs/08`）。
4. **問題集フェーズ1・2（完了）**：`problem_sets/1st_stage/` に7科目（A〜G）の論点別見出しを作成し、2016〜2025年度（直近10年）の過去問全問を分析して各論点に頻出ランク（A/B/C）と出題年度を確定済み。作業は科目ごとにサブエージェントを並列実行して行った（各科目 `problem_sets/1st_stage/<記号>_<科目名>.md` を参照）。
5. **問題集フェーズ3（一巡完了）**：`problem_sets/1st_stage/exercises/` に7科目×6問（計42問）の演習を作成済み。B・C科目でフォーマットを試作しユーザー承認を得た後、A・D・E・F・Gに同じフォーマット（設問→`<details>`で折りたたんだ解答・全選択肢の解説→関連知識）で展開した。各科目6問は代表例であり、Aランク論点はまだ多数未収録。展開時はサブエージェントに既存演習ファイルをテンプレートとして渡し「不確かな設問は避け正確性を最優先」と明示する方法が有効（計算問題は作成者が検算して確認済み）。
6. **学習スケジュール**：`docs/10_study_schedule.md` に次回試験（令和9年度想定）から逆算した5フェーズの学習計画・科目別優先順位を作成済み。
7. **初心者向け教科書（7科目完成）**：`docs/textbook/<記号>_*_textbook.md` に第1次試験7科目（A:経済学36論点、B:財務・会計34論点、C:企業経営理論42論点、D:運営管理35論点、E:経営法務27論点、F:経営情報システム24論点、G:中小企業経営・政策28論点、計226論点）の初心者向け解説を作成済み。C科目で試作したフォーマット（基本概念→具体例→ひっかけポイント→過去問で確認する、の繰り返し）を全科目に展開。計算問題は既存演習ファイルの数値と整合するよう作成・検算し、専門知識はWeb検索で正確性を確認した。ユーザーからの要求水準は「これを読んで理解すれば、問題集が100点取れる資料」。
8. **スライドテンプレートの仕様確定（デザイン検証完了）**：教科書を図解の多いスライドにする方向性を検討し、5デザイン案（Swiss Grid／Soft Pastel／Editorial Serif／Blueprint／Neubrutalist／Ink&Washi）から**Swiss Grid**（モノクロ+赤1色、罫線グリッド）を採用。汎用図解パターン12種＋科目別専用グラフ21種（経済学6・財務3・企業経営3・運営管理3・情報3）を試作し、仕様を `docs/11_slide_template_spec.md` に確定。サンプル実体は `docs/slide_template/*.html`（単体HTMLでブラウザ直接閲覧可）。
   - **重要な実装教訓**：スマホ実機での確認で「グラフがヘッダーに重なる」「文字がはみ出す」不具合が繰り返し発生し、原因を特定して修正済み。①カードの16:9固定は`aspect-ratio`プロパティ単体でなく`padding-top`%トリックで実装する、②カード内の文字サイズは固定pxでなくCSSコンテナクエリ（`cqw`単位、`container-type:inline-size`）でカード幅に追従させる、③SVGグラフの枠は高さを強制せず`object-fit:contain`と同じ考え方で「収まるだけ縮む」実装にする（`max-width:100%;max-height:100%;width:auto;height:auto`）。次回スライド関連の実装をする際は必ず `docs/11_slide_template_spec.md` の「実装上の注意点」を読んでから着手すること。
   - なお `scripts/slidegen/`（python-pptx製）でPowerPoint版も先行試作したが、生成物がPowerPointで「修復が必要」エラーになる不具合が未解決のままHTML版に方針転換した経緯がある。
   - このデザイン検討の全経緯（却下したデザイン案、モバイル実機バグの3段階にわたる原因究明の過程など）は `docs/12_slide_design_session_log.md` に詳細を記録している。次回スライド関連の作業をする前に一読すること。
9. **文章主体パターン（T1〜T3）の確定**：図解パターンの試作を終えた後、「教科書の論点の大半はグラフで描けるものではなく文章中心」という点に気づき、図解を小さく添えるだけ／図解なしの3パターン（T1:概念解説＋小アイコン、T2:よくある混同の整理＋警告マーク、T3:罫線＋番号だけの箇条書きリスト）をC科目の実内容（ドメイン、CSR/CSV、モチベーション理論）で試作し `docs/slide_template/text_primary_3.html` として確定。本番スライド作成では論点の性質に応じて図解主体（12汎用＋科目別グラフ）と文章主体（T1〜T3）を使い分ける方針を `docs/11_slide_template_spec.md` に明記した。
   - **実装上の教訓（グラフと同一クラスのバグ）**：小アイコンや箇条書き行でも「固定の高さ・幅」「`justify-content:center`」を使うと、グラフの時と全く同じ原因（縮まずあふれ、上＝ヘッダー側に重なる）でモバイル実機バグが再発した。修正も同じ原則（`height:100%`＋`max-height`でアイコンを親に追従させる、`justify-content:flex-start`＋`overflow:hidden`＋`min-height:0`で万一のあふれを下端に逃がす）で解決した。
   - **この環境でのビジュアル自己検証の発見**：Node.js版Playwright＋Chromium（`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`）がこの環境で使用可能（`npm install playwright`でセットアップ、ブラウザ再ダウンロード不要）。ユーザーにスクリーンショットを撮ってもらわなくても、自分でHTMLをレンダリング・スクリーンショットして確認できる。次回以降、スライド関連のレンダリング確認をする際はこの方法を使うこと（LibreOffice/`soffice`はこの環境で動作しないので注意、これとは別の話）。
10. **「頻出データ帯」の考案と白空間問題の解決**：一通りのパターンが揃った後、ユーザーから「厳しく評価して」と求められ、既存サンプル（4P、SWOT、需要供給曲線等）を見返した結果「カードの上半分〜6割近くが空白」という弱点を正直に指摘した。対策として`docs/textbook/C_business_administration_textbook.md`のC-7・C-8を**要約せず全文そのまま**流し込んで検証したところ、白空間の原因はモック文章が短いからではなく**16:9という箱が1論点あたりの文章量に対して構造的に大きい**ことだと判明。ユーザーの「スライドの16:9は絶対維持して、情報量を増やして」という指示を受け、`problem_sets/1st_stage/<科目>.md`に既にある実データ（頻出ランクA/B/C・出題年度）を使った「頻出データ帯」（頻出ランクチップ＋関連論点＋2016〜2025年度10年分を●/○で示す出題年表）を全パターン共通の標準要素として追加し、`docs/slide_template/freq_data_bar.html`として確定。捏造データではなく実データで埋めたことで白空間と情報価値の両方を解決した。
   - **不採用にした案**：白空間対策として、文章主体パターンを左右2列・上下2段に分割して情報量を倍にする「T4/T5」も試作したが、文字を大幅に縮小しないと収まらず可読性を犠牲にするため不採用とした（頻出データ帯だけで十分解決できたため）。
   - **教訓**：`cqw`は「どの要素のコンテナクエリを基準にしているか」で意味が変わる（列を分割したら列自体を新たなコンテナにしないと文字は縮まない、かつcqwは幅基準なので高さ方向の分割には効かない）。また、モックの見た目だけで判断せず、早い段階で実データ（要約せず全文）を流し込んで検証すること。詳細経緯は`docs/12_slide_design_session_log.md`の10章・11章参照。

### 未着手・今後の拡充候補

- **演習の拡充**：`problem_sets/1st_stage/exercises/` の各科目6問を、`problem_sets/1st_stage/<科目>.md` の未収録Aランク論点に向けて追加していく。教科書側は全論点をカバー済みだが、対応する演習が手薄な論点が多いため優先順位が高い。
- **第2次試験の問題集化・教科書化**：`problem_sets/` および `docs/textbook/` は現状1次試験のみ。事例I〜IVは論述式のため論点別ランク付けになじまず、別の切り口（頻出テーマ・フレームワーク別の演習集など）を検討する必要がある。
- **令和8年度第2次試験**（2026年10月25日実施予定）の過去問・出題の趣旨・統計は実施後に追加すること。
- **スライドの本番作成**：`docs/11_slide_template_spec.md` の仕様（図解主体12種＋科目別グラフ21種＋文章主体3種＋頻出データ帯）は確定し、実コンテンツでの検証（C-7・C-8）も完了した。`docs/textbook/` の全226論点を通しで本番スライド化する作業はこれから。
- **経営法務(E)・中小企業経営政策(G)の専用グラフ**：他5科目分は作成済みだが、この2科目分の専用パターンは未着手（法令の存続期間比較や中小企業基本法の分類基準表など、法律・政策系ならではの見せ方の検討が必要）。

## よくある作業パターン

- 新しい年度のPDFを追加する場合：`past_exams/README.md` のファイル名対応表（A〜G＝科目、A〜D＝事例）を参照し、`docs/05_sources.md` に出典を追記すること。
- 論点別頻出ランクを再集計する場合：`problem_sets/1st_stage/<科目>.md` の「集計時の参照先」欄にPDFのパスパターンが書いてあるので、そこから辿る。
