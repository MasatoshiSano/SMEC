{
  "name": "smec",
  "version": "0.1.0",
  "private": true,
  "description": "SMEC starter web application (API + frontend) used to bootstrap the Cloud Agent development environment.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "lint": "node --check src/app.js && node --check src/server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
